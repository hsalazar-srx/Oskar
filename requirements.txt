# OSKAR — Python dependencies
# Pin versions before Sprint 1 begins

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Task queue (Redis eliminated — ADR-007; PostgreSQL broker via celery[sqlalchemy])
celery[sqlalchemy]==5.4.0
psycopg2-binary==2.9.9  # Kombu PostgreSQL transport requires sync psycopg2 (not asyncpg)

# State machine (ADR-002, F-7)
transitions==0.9.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
ldap3==2.9.1

# HTTP client (Movex adapter, supplier APIs)
httpx==0.28.1

# Resilience (F-5 — ERP adapter retry + circuit breaker)
tenacity==9.0.0
pybreaker==1.2.0

# Email notifications (PRE-9)
aiosmtplib==3.0.2

# Validation
pydantic==2.10.3
pydantic-settings==2.7.0

# File upload parsing (bulk item import)
python-multipart==0.0.20  # FastAPI multipart/form-data support
openpyxl==3.1.5           # .xlsx parsing (server-side authoritative validation)
chardet==5.2.0            # CSV encoding detection (CP1252 from AS/400/MOVEX exports)

# Observability (P0-4, ai/memory/11)
structlog==24.4.0

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==5.0.0
pre-commit==4.1.0
pip-audit==2.8.0
